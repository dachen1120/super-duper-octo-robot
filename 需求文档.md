# 产品需求文档（PRD）

## 「行业新闻 · 美式英语学习」网站（工作名：NewsTalk / 用兴趣学英语）

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 行业新闻 · 美式英语学习（工作名：NewsTalk） |
| 文档版本 | v0.2 |
| 编写日期 | 2026-09-07 |
| 状态 | 评审中（OQ-01 已确认：全站美式英语） |
| 技术栈方向 | Next.js + DeepSeek API（Key 走环境变量）+ 前端状态（v1 无数据库） |
| 关联仓库 | 本仓库根目录（site / content / scripts / tools） |
| 修订记录 | v0.2（2026-09-07）：产品决策——全站统一采用美式英语（en-US），单词发音由 en-GB 改为 en-US（rate=0.85） |

> 修订说明：本文档依据需求方提供的《产品概述与功能要求》整理，补充了内容模型、技术方案、验收标准与开放问题，作为后续开发与验收的基准。标注为「待确认」的条目需在评审会上拍板。

---

## 1. 产品概述

### 1.1 一句话定位

一个「以行业最新新闻为素材、以 A/B 朋友式双语对话为载体的美式英语听力 + 口语练习」Web 应用：用户先选感兴趣的行业，再进入该行业最新新闻的双语对话学习页，通过 **A/B 双人对话、逐句精听、单词学习、语法讲解、跟读录音对比** 等功能，练习英语听力与口语。

### 1.2 背景与动机

- 传统新闻英语学习内容「旧、泛、散」：素材不是用户关心的行业、更新慢、无口语训练闭环。
- 用户希望“用兴趣驱动”：先选行业（AI、财经、体育……），再看与自己兴趣相关的真实新闻，学习动力更强。
- 现有工具普遍缺少「可跟读、可对比、逐句精听」的口语练习闭环，且难以提供「朋友聊天」式的自然语料。

### 1.3 产品价值主张

| 用户痛点 | 本产品解法 |
| --- | --- |
| 新闻素材不匹配兴趣 | 行业卡片自由选择，随兴趣切换 |
| 内容过时 / 不真实 | 抓取最近 24–72 小时真实新闻，每条标注来源媒体 + 原文 URL + 更新时间 |
| 听力材料“念报纸”不自然 | 统一生成 A/B 朋友聊天式对话，语料口语化、带情绪 |
| 听不懂、查词打断节奏 | 英文句后紧跟中文翻译；点击单词即出中文释义并朗读发音 |
| 看完就忘、不会说 | 逐句精听 + 角色扮演跟读 + 录音对比三件套形成练习闭环 |

### 1.4 目标用户

1. 初级 ~ 中级英语学习者（约 CET-4 至 CET-6 / 雅思 5.5–6.5 水平），希望提升听力与口语；
2. 对特定行业（AI 科技、财经、汽车新能源等）有信息获取需求的职场人 / 学生；
3. 偏好移动端碎片化学习，同时使用桌面端深度练习。

### 1.5 关键名词定义

| 名词 | 定义 |
| --- | --- |
| 行业（Industry） | 内容分类维度，首页以卡片展示，可增删（见 §3.1、§4） |
| 新闻条目（News Item） | 单一行业下的一条真实新闻：标题、摘要、来源、URL、更新日期 |
| A/B 对话（Dialogue） | 两位角色（A 消息灵通型、B 好奇型）围绕新闻的双语聊天 |
| Turn（话轮） | 对话中 A 或 B 说的一句话，含英文、中文、释义表、语法讲解等 |
| 逐句精听 | 按句点击播放、播放句高亮、自动滚动定位到当前句的学习模式 |
| Shadowing / 角色跟读 | 选中某角色后，对方话轮正常播放、本方话轮静音留白供用户开口 |
| 跟读对比 | 先播原音 → 自动录音 → 提供「听原音 / 听我的 / 重录」对比 |

---

## 2. 核心流程与用户故事

### 2.1 核心流程

```
① 首页（行业选择）
   └─ 展示 ≥11 个行业卡片（AI科技、互联网、财经金融、汽车新能源、医疗健康、
      消费零售、体育、娱乐影视、教育、旅游、航天航空…）
② 用户点击某个行业卡片
   └─ 进入该行业「学习页」
       ├─ 展示该行业最近 24–72h 的 3–6 条重要新闻（重要性从高到低）
       ├─ 生成 / 展示 A/B 双语对话（英文 ≤15 词/句，英文后紧跟中文）
       ├─ 学习工具：播放控制条 / 逐句精听 / 点词释义+发音 / 语法讲解 / 口语练习
③ 用户随时点击「返回首页 / 切换行业」
   └─ 回到首页选择其他行业，流程循环
```

### 2.2 用户故事

| ID | 角色 | 用户故事 |
| --- | --- | --- |
| US-01 | 学习者 | 打开网站先看到行业卡片，能一眼找到我最关心的「AI 科技」并点击进入 |
| US-02 | 学习者 | 进入行业后能看到最新 3–6 条新闻、来源媒体和原文链接，并相信内容是真实的 |
| US-03 | 学习者 | 能按 A/B 对话从头到尾听一遍，语速可调、可暂停、进度可拖 |
| US-04 | 学习者 | 某句没听懂时能点击该句单独重听，句子播放时会高亮并自动滚到可见位置 |
| US-05 | 学习者 | 遇到生词点击即看中文释义，并听到单词发音 |
| US-06 | 学习者 | 想看某句结构时，点「语法」按钮展开语法讲解 |
| US-07 | 学习者 | 练习口语时能选 A 或 B 角色进行 shadowing（对方话轮正常播放、本方话轮静音留白） |
| US-08 | 学习者 | 能单句循环播放，也能录下自己跟读并与原音对比、重录 |
| US-09 | 学习者 | 学完想换个行业时，随时能返回首页切换 |

---

## 3. 功能需求

### 3.1 首页（行业选择）

**FR-01 行业卡片列表**
- 展示多个行业卡片；首批至少包含：AI 科技、互联网、财经金融、汽车新能源、医疗健康、消费零售、体育、娱乐影视、教育、旅游、航天航空（≥11 个）。
- 行业可后续增删（建议做成可配置清单，见 §6.5 / 内容模型 Industry）。
- 每个卡片建议展示：行业图标/Emoji、行业中文名、（可选）英文名、（可选）新闻条数或更新时间提示。

**FR-02 进入学习页**
- 点击行业卡片 → 跳转到对应行业学习页；URL 建议形如 `/industry/[industryId]`。
- 页面加载态需清晰（骨架屏或 loading 文案），加载失败给出重试入口。

**FR-03 返回首页**
- 学习页顶部提供「← 返回首页 / 切换行业」入口，随时可返回首页。

### 3.2 学习页整体

**FR-04 页面构成**
学习页自上而下包含：

1. 顶部导航栏（返回首页 / 切换行业 + 当前行业名）；
2. Sticky 播放控制条（见 FR-06）；
3. 新闻概览区：3–6 条新闻卡片（标题、摘要、来源媒体名 + 原文链接、更新日期时间），按重要性从高到低排序；
4. 对话学习区：A/B 双人对话气泡流，逐句展示英文 + 中文翻译 + 学习操作。

**FR-05 新闻区展示规范**
- 每条新闻展示：重要度序号（① 最高）、主题标签（如 #AI #融资）、来源（媒体名称，可点击跳转原文 URL，建议 `target="_blank" rel="noopener noreferrer"`）、更新日期时间（精确到小时：`YYYY-MM-DD HH:mm` 或相对时间 + 绝对时间）。
- 来源信息必须真实，链接指向原文；无法核实的内容不得展示（见 §4.1）。

### 3.3 播放控制条（Sticky）

**FR-06 播放控制条**
- 位置：学习页顶部，`position: sticky; top: 0`，**滚动时不消失**（置于导航栏之下或合并）。
- 控件：
  1. 播放 / 暂停按钮（▶/⏸）；
  2. 可拖动的进度条（拖动可 seek 到任意位置，拖动时预览时间，松开后跳转）；
  3. 时间显示（当前时间 / 总时长）；
  4. 语速调节：提供 0.75x / 1x / 1.5x 三档切换；
  5. （可选）上一句 / 下一句按钮。
- 播放控制条控制的对象是「当前对话音频」（整段 A/B 对话朗读音频，见 §4.4），逐句播放复用同一音频的指定时间区间（见 FR-07）。

### 3.4 逐句精听

**FR-07 逐句点击播放 + 高亮 + 自动滚动**
- 每条对话话轮（Turn）展示为一个句子块：英文大字号在上、中文翻译紧随其下（见 FR-10）。
- 点击某句 → 从该句起点播放至该句结束（或循环播放该句，见 FR-12）。
- 播放中的句子高亮（气泡描边/底色变化），**停止或切换时高亮跟随当前句**。
- 自动滚动定位当前句：**必须使用 `getBoundingClientRect()` 计算目标句位置 + `window.scrollTo({top, behavior:'smooth'})`；禁止使用 `scrollIntoView()`**。
- 高亮句始终尽量保持在可视区域内（若句子在视口内则不滚动，避免打扰）。

### 3.5 单词学习

**FR-08 点词释义 + 发音**
- 英文句子中的单词（至少全实词）可点击。
- 点击单词：弹出/高亮显示中文释义（释义来自该句的「全实词释义表」，见 §4.3 / §5）。
- 点击发音：使用 Web Speech API `speechSynthesis`，`lang='en-US'`，`rate=0.85` 朗读该单词（已按产品决策统一为美式英语，见 OQ-01）。
- 交互不打断整段/逐句播放进度；释义气泡可再次点击关闭。

**FR-09 释义表展示**
- 每句提供「全实词释义表」（可展开），列出句中所有实词：单词、词性、中文释义、（可选）句中含义/音标。

### 3.6 语法讲解

**FR-10 语法讲解**
- 每个句子块提供「语法」按钮，点击展开该句的语法讲解（中文讲解为主，含关键结构拆解、例句中用法说明）。
- 讲解内容由内容生成环节产出并随句子存储（见 §4.3），不要求前端实时计算。

### 3.7 口语练习

**FR-11 单句循环**
- 每个句子块提供「🔁 循环」按钮：点击后该句循环播放，直到再次点击停止或切到其他句。

**FR-12 A/B 角色切换（Shadowing 角色扮演）**
- 每个句子块（或全局模式）允许选择角色 A 或 B。
- 选中 A：A 的话轮正常播放；B 的话轮时段**静音留白**（保留停顿时长），界面同步高亮提示「该你开口了」（B 的句子）。
- 选中 B：反之。
- 角色切换后，播放控制条按新规则播放。

**FR-13 跟读录音对比**
- 每个句子块提供「🎤 跟读」按钮，流程：
  1. 点击「🎤 跟读」→ 先播放该句原音；
  2. 原音结束后自动开始录音（需麦克风权限，浏览器 navigator.mediaDevices；HTTPS/localhost 下可用）；
  3. 录音结束（自动按句子时长截断或用户手动停止）后，出现三个按钮：「听原音 / 听我的 / 重录」；
  4. 「听我的」回放本次录音；「重录」清空本次录音重新走 1–3。
- 状态机需清晰：空闲 → 播原音 → 录音中(倒计时/波形可选) → 可对比 → 重录。
- 录音数据保存在当前页面状态（内存）中，不强制上传（v1 无后端存储）。

### 3.8 内容与语音生成入口（产品层）

**FR-14 内容加载**
- 进入行业学习页后，系统获取该行业最新 3–6 条新闻并生成 A/B 对话与学习素材；生成过程需有进度/状态提示，并支持失败重试（网络、限流等）。
- v1 结果仅保存在当前页面状态（刷新后重新生成或使用缓存策略，见 §6.3）。

### 3.9 移动端与桌面适配

**FR-15 响应式**
- 桌面端与移动端均可用；字号、间距、按钮命中区（≥44px 建议）适配移动端。

---

## 4. 内容需求（内容生成规范）

### 4.1 新闻筛选与真实性（CR）

**CR-01 时效范围**
- 每个行业搜集**最近 24–72 小时**的重要新闻动态。

**CR-02 条数与排序**
- 筛选 **3–6 条**最有价值信息，按重要性从高到低排序，页面上以 ①②③… 标识。

**CR-03 来源与真实性（硬性要求）**
- 每条新闻必须标注：来源媒体名称 + 原文 URL + 更新日期时间（精确到小时）。
- **内容必须真实可溯源，严禁编造**；无法确认来源与时效的信息不得上屏。
- 建议在页脚/内容区标注免责声明：新闻摘要与对话由 AI 基于所引来源生成，学习用途，请以原文为准。

**CR-04 摘要与标签**
- 每条新闻提供简短中文/英文摘要（供对话区上下文与用户浏览），可附主题标签（#关键词）。

### 4.2 A/B 双语对话生成规范（CR）

对话风格为「朋友聊天」，**严禁念报纸**。

**CR-05 角色人设**
- A：消息灵通、热情、信息量足，负责抛出并讲解新闻；
- B：好奇听众，会追问与感叹，如「No way!」「Wait,」「Why though?」「That's insane.」。
- 允许短句和不完整句；话轮转换自然；开头有问候、结尾有总结。

**CR-06 内容覆盖**
- 每条新闻都要通过 A/B 的自然互动“聊出来”，来源（媒体/网址表述）在对话中自然带出，不机械念链接。

**CR-07 句长与双语格式**
- 每句英文尽量不超过 15 个单词。
- 展示格式：**英文在前，紧接中文翻译**。
- 语言难度适配初级~中级学习者；专业术语允许出现但需在释义表/语法中解释。

**CR-08 每句附属内容**
每句（Turn）附带：
1. 全实词中文释义表（见 CR-09）；
2. 语法讲解（见 CR-10）。

**CR-09 全实词释义表**
- 覆盖句中全部实词（名词/动词/形容词/副词等；虚词如冠词、介词、连词视难度可选收录，但实词必须全收）。
- 字段：单词（原形优先，变形词标注原形）、词性、中文释义、（可选）音标、句中义。

**CR-10 语法讲解**
- 每句一条简短语法讲解（中文），说明：句型结构、时态/语态、关键搭配或从句关系等，便于学习者理解该句。

### 4.3 生成结果的结构化要求（CR）

- DeepSeek 生成结果必须为**结构化 JSON**（schema 见 §5），字段齐全，前端可直接渲染；
- 生成前给模型提供新闻原文/摘要与来源；模型负责改写为对话与学习素材，**不得新增新闻事实**；
- 建议对生成结果做格式校验（缺字段/超长句/角色非法则重新生成或降级提示）。

### 4.4 朗读音频规范（CR）

**CR-11 双人朗读音频**
- 整段对话生成一份（或按句切分）美式英语双人朗读音频：
  - A：青年男声（美音）；B：青年女声（美音）；
  - 发音清晰、语速适中偏慢、句间有停顿、B 的语气带情绪（惊讶/好奇）。
- 每句（Turn）需有可定位的起止时间（时间戳），以支持逐句播放、高亮、留白 shadowing（FR-07/FR-12）。
- 技术路线建议：edge-tts / Azure 神经语音（en-US 男女声）离线或服务端预生成，句间插入停顿并按句切分打点（仓库 scripts/tools 已有 edge-tts 实验基础）。

**CR-12 单词发音**
- 单词发音用 Web Speech API，`lang='en-US'`、`rate=0.85`（产品决策：全站统一美式英语，见 OQ-01）。

---

## 5. 数据与内容模型（Schema 草案）

> v1 不落数据库，模型用于定义「内容生成 prompt 的输出结构」与「前端状态结构」，并为 v2 入库预留。

```jsonc
// Industry 行业
{
  "id": "ai-tech",                 // 行业唯一 id（URL 用）
  "name": "AI科技",
  "nameEn": "AI & Tech",
  "icon": "🤖",                     // 首页卡片图标
  "sort": 1
}

// NewsItem 新闻条目（一个行业 3–6 条，按 importance 降序）
{
  "id": "n-20260907-01",
  "industryId": "ai-tech",
  "importance": 1,                  // 1 最高
  "headline": "……",                // 标题
  "summary": "……",                 // 摘要（中英可选）
  "tags": ["AI", "融资"],
  "source": "TechCrunch",           // 来源媒体名称
  "sourceUrl": "https://…",         // 原文 URL
  "updatedAt": "2026-09-07T10:30:00+08:00"
}

// Turn 话轮（对话中的一句）
{
  "id": "t-0001",
  "speaker": "A",                   // "A" | "B"
  "en": "OpenAI just dropped a brand new model, and it is seriously fast.",
  "zh": "OpenAI 刚发布了一款全新模型，速度快得惊人。",
  "audio": { "startMs": 0, "endMs": 4200 },   // 在该句/整段音频中的区间
  "glosses": [                      // 全实词释义表
    { "word": "dropped", "lemma": "drop", "pos": "v.", "meaning": "发布；投放（口语）" }
  ],
  "grammar": { "title": "现在完成时 + and 并列", "body": "……中文讲解……" }
}

// Dialogue 行业当日对话（一个行业一次生成）
{
  "industryId": "ai-tech",
  "title": "AI 圈今天发生了什么？",
  "generatedAt": "2026-09-07T11:00:00+08:00",
  "news": [ /* NewsItem[]，3–6 条 */ ],
  "turns": [ /* Turn[]，含问候开头与总结结尾 */ ],
  "audio": {
    "src": "blob:… 或 /api/tts/…",  // 整段音频
    "durationMs": 180000,
    "speeds": { "0.75": …, "1": …, "1.5": … }   // 由播放器 rate 实现即可
  }
}
```

---

## 6. 技术需求

### 6.1 技术选型

| 需求 | 选型 / 方案 |
| --- | --- |
| 前端框架 | Next.js（App Router 或 Pages Router，团队自定，建议 App Router） |
| AI 内容生成 | DeepSeek API（对话/结构化输出），用于新闻 → A/B 对话 + 学习素材 |
| 密钥管理 | DeepSeek API Key 放**环境变量**（`.env.local`，如 `DEEPSEEK_API_KEY=…`），加入 `.gitignore`，**绝不硬编码/入库/进前端 bundle** |
| 数据存储 | v1 **不使用数据库**；结果仅保存在当前页面状态（React state / 内存），见 §6.3 |
| 朗读音频 | edge-tts / Azure 神经语音（en-US 男女声）生成整段与分句音频 + 时间戳（仓库已有 edge-tts 实验）；单词发音用浏览器 Web Speech API |
| 样式 | CSS Modules / Tailwind 自选；遵循 §7 视觉规范 |

### 6.2 路由规划（建议）

| 路由 | 说明 |
| --- | --- |
| `/` | 首页：行业卡片选择 |
| `/industry/[industryId]` | 行业学习页：新闻 + A/B 对话学习 |
| `/api/generate`（内部） | 代理调用 DeepSeek，Key 仅存在于服务端；入参：行业 id / 新闻素材；出参：结构化 Dialogue JSON |
| `/api/tts`（内部，可选） | 服务端 TTS 生成/缓存音频，返回音频 URL 或分段时间戳 |

> 说明：若行业内容支持「后端抓新闻」，需另行增加新闻抓取/检索服务（v1 可先由人工/脚本准备新闻素材或调用搜索 API 获取，见 OQ-03）。

### 6.3 内容获取与状态（v1）

1. 进入学习页 → 前端请求 `/api/generate?industry=…`；
2. 服务端读取环境变量中的 DeepSeek Key → 携带新闻素材 prompt → 返回结构化 Dialogue；
3. 前端将 Dialogue（含新闻、话轮、音频）保存在**当前页面状态**（React state）；
4. 刷新页面后状态丢失，则重新请求（可加浏览器端内存缓存或 localStorage 短缓存提升体验，非必需）；
5. 首次使用无数据库，不做账号/历史/收藏（见 v2 规划 §8.2）。

### 6.4 语音技术要点

- 逐句定位：以整段音频 + 每句 `startMs/endMs` 实现；播放器 `currentTime` 落到某句区间即高亮该句（FR-07）。
- 语速：优先用 HTMLAudioElement `playbackRate`（0.75/1/1.5）实现，避免生成三份音频。
- 录音：`navigator.mediaDevices.getUserMedia({audio:true})` + `MediaRecorder`；**需要 HTTPS 或 localhost**；需处理权限拒绝/浏览器不兼容（Safari 等）降级提示。
- Web Speech：`speechSynthesis.speak(new SpeechSynthesisUtterance(word))`，`lang='en-US'`、`rate=0.85`；注意 Chrome/Edge 需要用户手势触发、语音包可用性。
- 自动滚动：`getBoundingClientRect()` + `window.scrollTo()`（FR-07，禁用 scrollIntoView）。

### 6.5 行业可配置（v1 起步）

- 行业清单建议由一份配置文件/常量维护（`industries.ts` / JSON），新增行业 = 增加配置 + 内容生成覆盖，无需改页面结构，满足「用户可后续增删」。

### 6.6 非功能需求（NFR）

| 编号 | 类别 | 要求 |
| --- | --- | --- |
| NFR-01 | 性能 | 首页首屏可交互 ≤ 2s（4G）；学习页内容加载有骨架/进度；音频可边下边播 |
| NFR-02 | 兼容 | 现代 Chrome / Edge / Safari（iOS）/ Firefox；移动端优先适配 |
| NFR-03 | 安全 | API Key 仅服务端；对外接口做入参校验与限流；来源链接外链安全处理 |
| NFR-04 | 无障碍 | 按钮有 aria-label/标题；播放/录音状态有文字提示；颜色对比度达标 |
| NFR-05 | 隐私 | 录音仅存于页面内存，不自动上传；隐私说明告知用户 |
| NFR-06 | 错误处理 | 生成失败 / TTS 失败 / 麦克风拒绝均有明确提示与重试 |
| NFR-07 | 可维护 | 内容 schema 与 prompt 版本化管理（本仓库 content / scripts 目录） |

---

## 7. 交互细节与视觉规范

### 7.1 主题与字体

- 白色浅色主题为主。
- 无衬线字体栈：`"Helvetica Neue", Arial, "Noto Sans SC", -apple-system, sans-serif`。
- 英文大字号：桌面 **22px**；移动端 **20px**；中文翻译字号建议略小（如 15–16px）并弱化颜色。

### 7.2 A/B 对话气泡

- A、B 角色气泡使用**不同底色**（如 A 蓝系浅底、B 绿/橙系浅底），气泡内标注角色名（A / B 或昵称）。
- 播放中高亮：气泡描边/底色加深；角色跟读留白时用醒目提示色提示「该你开口」。

### 7.3 标签与来源

- 新闻主题标签：小圆角 pill 样式（如 `#AI`、`#融资`）。
- 来源标注：媒体名称做成可点击链接（打开原文 URL），旁边显示更新时间（`MM-DD HH:mm` 或相对时间）。

### 7.4 播放器

- Sticky 顶部固定（FR-06）；进度条可拖动；当前句进度可在对话区高亮句上体现。
- 语速档位按钮 0.75x / 1x / 1.5x，当前档位高亮。

### 7.5 交互状态机（录音）

```
idle
 └─(点🎤跟读)─> playingOriginal(播原音)
                └─(原音播完)─> recording(录音中，显示计时/波形)
                               ├─(到句尾自动停/用户点停)─> comparing(听原音|听我的|重录)
                               └─(点取消)─> idle
comparing ─(点重录)─> playingOriginal（重新开始）
```

---

## 8. 里程碑与范围

### 8.1 v1（MVP，本次目标）

| 模块 | 交付物 |
| --- | --- |
| 首页 | 行业卡片列表（≥11 行业，配置文件维护），点击进入 |
| 学习页 | 新闻区（3–6 条、来源+链接+更新时间）+ A/B 对话区（英文+中文） |
| 播放 | Sticky 播放条：播放/暂停/进度拖动/0.75x·1x·1.5x |
| 逐句精听 | 点击播放该句、播放高亮、getBoundingClientRect+window.scrollTo 自动滚动 |
| 单词 | 点词释义 + Web Speech（en-US, 0.85）发音 |
| 语法 | 每句「语法」按钮展开讲解 |
| 口语 | 🔁循环、A/B 角色 shadowing（留白+高亮）、🎤跟读录音对比 |
| 语音 | 美音 A 青年男声 + B 青年女声整段音频 + 句时间戳（edge-tts/服务端生成） |
| 内容 | DeepSeek 生成管线 + 结构化 JSON（schema §5）+ 真实新闻来源规范 |
| 工程 | Next.js、环境变量 Key、无数据库、页面状态 |

### 8.2 后续版本（v2+，非本期）

- 行业自定义增删（用户侧）、多行业收藏；
- 数据库持久化：历史会话、学习记录、收藏、生词本；
- 更丰富的口语评测（语音打分）、发音纠错；
- 每日定时自动更新新闻的自动化流程；
- 播放进度/学习进度跨设备同步、账号体系。

---

## 9. 风险与开放问题

| 编号 | 问题 | 说明 / 建议 |
| --- | --- | --- |
| OQ-01 | 口音不一致（已确认 ✅ 2026-09-07） | 决策：**全站统一采用美式英语（en-US）**。主音频与口语训练为美音 A/B 男女声；单词发音由 en-GB 改为 en-US（rate=0.85）；正文不再出现「英式英语」。已同步更新 FR-08、CR-12、§6.4、§8.1、AC-07 |
| OQ-02 | 新闻真实性与时效保障 | 「24–72h 真实新闻 + 3–6 条」依赖抓取/检索源质量。建议 v1 明确新闻素材来源方案（人工整理脚本 / 新闻搜索 API），并在 UI 明示「AI 生成摘要，以原文为准」 |
| OQ-03 | 内容生成链路归属 | DeepSeek 负责「新闻 → 对话与学习素材改写」，不负责事实核查；新闻抓取或检索是否需要独立服务，v1 建议由脚本/API 预取 |
| OQ-04 | TTS 成本与延迟 | 服务端 TTS 生成整段对话有耗时/成本，建议缓存 + 分句渐进生成；评估 edge-tts 免费方案 vs Azure 付费音质 |
| OQ-05 | 浏览器录音/语音兼容 | 录音需 HTTPS；Web Speech 发音依赖系统语音包，不同浏览器/系统可用性不同，需降级提示 |
| OQ-06 | 生成稳定性 | DeepSeek 输出可能缺字段/超长句/角色错误，需 JSON schema 校验与重试机制 |
| OQ-07 | 免责与版权 | 对话基于媒体新闻改写，属学习用途；建议页面展示来源链接与免责声明 |

---

## 10. 验收标准（节选）

### 10.1 功能验收

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| AC-01 | 首页行业卡片 | ≥11 个行业卡片可见，点击任意卡片进入对应学习页，URL 含行业 id |
| AC-02 | 新闻区 | 展示 3–6 条新闻，含标题、来源媒体（可点击打开原文新标签）、更新日期时间、重要性排序标识 |
| AC-03 | 真实性 | 抽查新闻均可溯源到原文 URL；页面含免责声明 |
| AC-04 | A/B 对话展示 | 每条新闻都被 A/B 对话覆盖；英文 ≤15 词/句（可容忍少量超出并记录）；英文在上中文紧跟在下；开头问候、结尾总结 |
| AC-05 | 播放控制条 | Sticky 固定不消失；播放/暂停、进度拖动 seek、0.75/1/1.5x 生效 |
| AC-06 | 逐句精听 | 点句播放该句；播放句高亮；自动滚动使用 getBoundingClientRect + window.scrollTo（代码审查确认无 scrollIntoView） |
| AC-07 | 点词 | 点击实词显示中文释义；触发 Web Speech en-US rate=0.85 发音 |
| AC-08 | 语法 | 每句「语法」按钮可展开对应讲解 |
| AC-09 | 循环 | 「🔁 循环」使当前句循环播放，可停止 |
| AC-10 | 角色 shadowing | 选中 A 时 B 话轮静音留白且高亮提示开口；选中 B 反之 |
| AC-11 | 录音对比 | 🎤 流程：播原音→自动录音→出现「听原音/听我的/重录」；重录可重新走流程 |
| AC-12 | Key 安全 | 代码中无明文 DeepSeek Key；Key 仅存在于环境变量/服务端 |
| AC-13 | 无数据库 | v1 无后端持久化依赖；刷新后重新生成可用 |
| AC-14 | 视觉 | 浅色主题、指定字体栈、英文 22px（桌面）/20px（移动端）、A/B 气泡异色 |

### 10.2 代码 / 仓库约定

- 新代码建议放 `site/`（Next.js 应用）、`scripts/`（内容生成与 TTS 脚本）、`content/`（生成结果样例/缓存）、`tools/`（本地依赖），与本仓库现有目录结构保持一致。
- 密钥管理遵循 §6.1；`.env.local` 不入库。

---

*（本文档为 v0.2，欢迎评审补充。OQ-01 口音问题已于 2026-09-07 确认：全站采用美式英语 en-US；其余开放问题 OQ-02~OQ-07 待评审。）*