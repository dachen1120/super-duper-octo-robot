# Netlify 构建配置（Next.js App Router + API 路由）
# 注意：不要手动设置 publish=.next，否则会禁用 Netlify Next Runtime
[build]
  command = "pnpm build"

[build.environment]
  NODE_VERSION = "22"

# NEXT_PUBLIC_ 环境变量已在 Netlify UI/CLI 设置